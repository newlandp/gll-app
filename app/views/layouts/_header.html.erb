<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to "Home", root_path, { :class => "navbar-brand" } %>
    </div>
    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <p class="navbar-text" style="color:white"> Welcome, <%= current_user.name %></p>
          <li><%= link_to "Dashboard", user_path(current_user.id) %> </li>
          <li><%= link_to "Profile", user_path(current_user.id) %> </li>
          <li><%= link_to "Settings", user_path(current_user.id) %> </li>
          <li><%= link_to 'Sign Out', session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Sign Up", new_user_path %></li>
          <li><%= link_to "Sign In", new_session_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>